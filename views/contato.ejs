<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contato</title>
    <%- include('partials/head') %>
    <link rel="stylesheet" href="/css/contato.css">
        <% if (typeof pageCss !== 'undefined') { %>
        <style>
            <%= pageCss %>
        </style>
        <% } %>
</head>

<body>
    <%- include('partials/header') %>
        <div id="contato">
            <div>
                <h2>
                    <%= titulo %>
                </h2>
                <hr>
            </div>
            <div class="contato-texto">
                <p><strong>Sinta-se a vontade para me enviar uma mensagem!</strong></p>
            </div>
            <div class="contato-form">
                <form action="/api/contato" method="post">
                    <input type="text" name="nome" placeholder="Seu nome" required>
                    <input type="email" name="email" placeholder="Seu e-mail" required>
                    <textarea name="msg" placeholder="Digite sua mensagem" required></textarea>
                    <button type="submit">Enviar</button>
                    <% if (typeof enviado !=='undefined' && enviado) { %>
                        <p style="margin-top: 5px; padding: 3px; color: green; border: dotted green;">Mensagem enviada
                            com sucesso! :)</p>
                    <% } else if (typeof erro !=='undefined' && erro) { %>
                        <% if (erro === 'env') { %>
                            <p style="margin-top: 5px; padding: 3px; color: red; border: dotted red;">O envio não pôde ser realizado porque as credenciais de e-mail não estão configuradas no ambiente. Configure as variáveis <code>email_user</code> e <code>email_pass</code> e tente novamente.</p>
                        <% } else { %>
                            <p style="margin-top: 5px; padding: 3px; color: red; border: dotted red;">Ocorreu um erro ao
                                enviar sua mensagem. Tente novamente mais tarde.</p>
                        <% } %>
                    <% } %>
                </form>
            </div>
            <div class="contato-links">
                <a href="mailto:anajulia3907@gmail.com">
                    <i class="fa-solid fa-envelope"></i>
                </a>
                <a href="https://www.linkedin.com/in/ana-gaspar-957775325/">
                    <i class="fa-brands fa-linkedin-in"></i>
                </a>
                <a href="https://github.com/anajgaspar">
                    <i class="fa-brands fa-github"></i>
                </a>
            </div>
        </div>
</body>

</html>